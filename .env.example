# =============================================================================
# REQUIRED - Cần thiết cho pipeline hoạt động
# =============================================================================

# LLM Configuration (REQUIRED - Tạo editing plan)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-pro

# Transcription (REQUIRED - Local Whisper only)
WHISPER_MODEL=base
# Available models: tiny, base, small, medium, large
# Khuyến nghị: base (cân bằng tốc độ và độ chính xác)

# NOTE: Requires local Whisper installation:
# pip install -U openai-whisper
# Also requires ffmpeg to be installed on your system

# Google Sheets (REQUIRED - Lưu transcript)
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
GOOGLE_SHEETS_CREDENTIALS=./google-credentials.json

# Pexels API (REQUIRED - Download B-roll footage)
PEXELS_API_KEY=your_pexels_api_key

# Pixabay API (REQUIRED - Download sound effects)
# Same API key as Pexels - Pixabay provides both images and sound effects
PIXABAY_API_KEY=your_pixabay_api_key
# Get API key from: https://pixabay.com/api/docs/
SOUND_EFFECTS_PROVIDER=pixabay
SOUND_EFFECTS_CACHE_ENABLED=true

# YouTube API (REQUIRED - Upload video cuối cùng)
YOUTUBE_CLIENT_ID=your_youtube_client_id
YOUTUBE_CLIENT_SECRET=your_youtube_client_secret
YOUTUBE_REDIRECT_URI=http://localhost:3000/oauth/callback

# =============================================================================
# LOCAL DEVELOPMENT - Cần thiết cho chạy local
# =============================================================================

# Storage (Local directories)
TEMP_DIR=./temp
CACHE_DIR=./cache

# Redis (Local - dùng Docker hoặc cài local)
REDIS_HOST=localhost
REDIS_PORT=6379

# Server
PORT=3000
NODE_ENV=development

# =============================================================================
# OPTIONAL - Có thể bỏ qua khi dev local, uncomment khi cần
# =============================================================================

# Notifications (OPTIONAL - Chỉ cần khi muốn nhận thông báo)
# Chọn một trong các phương thức: telegram, webhook, email, sms
NOTIFICATION_METHOD=telegram

# Telegram Bot (Khuyến nghị - Dễ setup nhất)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
# Hướng dẫn lấy token: https://core.telegram.org/bots#6-botfather
# Hướng dẫn lấy chat ID: Gửi tin nhắn cho bot, sau đó truy cập:
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates

# Webhook (Alternative)
# NOTIFICATION_ENDPOINT=https://discord.com/api/webhooks/your-webhook-id

# Email operator (OPTIONAL - Nhận cảnh báo lỗi)
# NOTIFICATION_OPERATOR_EMAIL=operator@example.com

# =============================================================================
# DEPLOYMENT ONLY - Chỉ cần khi deploy production
# =============================================================================

# Cloud Storage (OPTIONAL - Chỉ khi deploy lên cloud)
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret
# AWS_REGION=us-east-1
# S3_BUCKET=your-bucket-name

# Redis Cloud (OPTIONAL - Chỉ khi deploy production)
# REDIS_URL=redis://user:password@host:port
# REDIS_TLS=true

# Monitoring (OPTIONAL - Chỉ khi cần monitoring)
# SENTRY_DSN=your_sentry_dsn
# DATADOG_API_KEY=your_datadog_key

# =============================================================================
# NOTES
# =============================================================================
# 1. Copy file này thành .env: cp .env.example .env
# 2. Điền các giá trị REQUIRED
# 3. Các phần OPTIONAL có thể bỏ qua khi dev local
# 4. Xem hướng dẫn chi tiết: docs/HUONG_DAN_ENV.md
# =============================================================================
