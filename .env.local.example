# =============================================================================
# LOCAL DEVELOPMENT - Minimal Setup
# =============================================================================
# Copy file này thành .env để bắt đầu: cp .env.local.example .env
# Xem hướng dẫn chi tiết: docs/HUONG_DAN_ENV.md
# =============================================================================

# 1. GEMINI API (Bắt buộc - Tạo editing plan)
# Lấy tại: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-pro

# 2. WHISPER (Bắt buộc - Transcription, chạy local)
# Không cần API key, chỉ cần cài: pip install openai-whisper
WHISPER_MODEL=base

# 3. GOOGLE SHEETS (Bắt buộc - Lưu transcript)
# Xem hướng dẫn: docs/HUONG_DAN_ENV.md phần Google Sheets
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
GOOGLE_SHEETS_CREDENTIALS=./google-credentials.json

# 4. PEXELS API (Bắt buộc - B-roll footage)
# Lấy tại: https://www.pexels.com/api/
PEXELS_API_KEY=your_pexels_api_key

# 5. YOUTUBE API (Bắt buộc - Upload video)
# Xem hướng dẫn: docs/HUONG_DAN_ENV.md phần YouTube
YOUTUBE_CLIENT_ID=your_youtube_client_id
YOUTUBE_CLIENT_SECRET=your_youtube_client_secret
YOUTUBE_REDIRECT_URI=http://localhost:3000/oauth/callback

# 6. LOCAL STORAGE (Tự động tạo nếu chưa có)
TEMP_DIR=./temp
CACHE_DIR=./cache

# 7. REDIS (Chạy local hoặc Docker)
# Docker: docker run -d -p 6379:6379 redis:7-alpine
REDIS_HOST=localhost
REDIS_PORT=6379

# 8. SERVER
PORT=3000
NODE_ENV=development

# =============================================================================
# OPTIONAL - Uncomment nếu cần
# =============================================================================

# Notifications (Nhận thông báo khi video xong)
# NOTIFICATION_METHOD=webhook
# NOTIFICATION_ENDPOINT=https://discord.com/api/webhooks/your-webhook-id

# =============================================================================
# QUICK START
# =============================================================================
# 1. Lấy 5 API keys ở trên (Gemini, Google Sheets, Pexels, YouTube)
# 2. Cài Whisper: pip install openai-whisper
# 3. Start Redis: docker run -d -p 6379:6379 redis:7-alpine
# 4. Copy file này: cp .env.local.example .env
# 5. Điền API keys vào .env
# 6. Run: npm install && npm run dev
# =============================================================================
